<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">
      <i class="bi bi-kanban"></i> TaskFlow
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <% if(isAuth){ %>
          <li class="nav-item">
            <a class="nav-link" href="/?view=list&lang=<%= currentLang %>" id="list-view-btn">
              <i class="bi bi-list-ul"></i> <%= __('navigation.list_view') %>
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/?view=kanban&lang=<%= currentLang %>" id="kanban-view-btn">
              <i class="bi bi-kanban"></i> <%= __('navigation.kanban_view') %>
            </a>
          </li>
        <% } %>
      </ul>
      <ul class="navbar-nav ms-auto">
        <!-- Language Switcher -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="languageDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="bi bi-globe"></i> 
            <% if (currentLang === 'ja') { %>日本語<% } else { %>English<% } %>
          </a>
          <ul class="dropdown-menu" aria-labelledby="languageDropdown">
            <li><a class="dropdown-item <%= currentLang === 'en' ? 'active' : '' %>" href="?lang=en<% if (typeof view !== 'undefined' && view) { %>&view=<%= view %><% } %>">🇺🇸 English</a></li>
            <li><a class="dropdown-item <%= currentLang === 'ja' ? 'active' : '' %>" href="?lang=ja<% if (typeof view !== 'undefined' && view) { %>&view=<%= view %><% } %>">🇯🇵 日本語</a></li>
          </ul>
        </li>
        <!-- Theme Toggle Button -->
        <li class="nav-item">
          <button class="nav-link btn btn-link text-white border-0 p-2" onclick="toggleTheme()" title="<%= __('Toggle Dark/Light Mode') %>" id="navThemeToggle">
            <i class="bi bi-moon-fill" id="navThemeIcon"></i>
          </button>
        </li>
        <% if(isAuth){ %>
          <li class="nav-item">
            <a class="nav-link" href="/logout">
              <i class="bi bi-box-arrow-right"></i> <%= __('navigation.logout') %>
            </a>
          </li>
        <% }else{ %>
          <li class="nav-item">
            <a class="nav-link" href="/signin">
              <i class="bi bi-box-arrow-in-right"></i> <%= __('navigation.signin') %>
            </a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
